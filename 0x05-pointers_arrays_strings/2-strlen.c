/*
 * File: 2-strlen.c
 * @Auth: Nyange
 *
 */
#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: string to evaluate
 *
 * Return: the length of the string
 */
int _strlen(char *s)
{
	int i = strlen(s);	

	return (i);
}
